services:
  eshopdb:
    container_name: eshopdb
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=EShopDb
    restart: always
    ports:
      - "5433:5432"   # avoid conflict
    volumes:
      - postgres_eshopdb:/var/lib/postgresql/data/